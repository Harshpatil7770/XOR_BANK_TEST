#Application.name
spring.application.name=eureka-server

#Profile
spring.profiles.active=dev

#port no
server.port=8761

#Actuator
management.endpoints.web.exposure.include=*

#log file generation path
logging.file.path=C:/Users/patil_ha/xor_bank_poc/logs/eurekaserver.logs

#Eureka Server doesn't register itself as client
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
